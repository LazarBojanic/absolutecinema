scene entrance(var args: string[]): scrap{
	var num: int = 25;
	var correct: bool = false;
	keepRollingIf(correct == false){
		var guess: int = capture();
		if(guess == num){
			correct = true;
		}
	}
	project(correct);
}