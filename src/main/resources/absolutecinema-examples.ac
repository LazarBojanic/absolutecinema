setup Shirt{
    var color: string;
    var size: int;
    Shirt(color: string, size: int){
        @.color = color;
        @.size = size;
    }
    scene getColor(): string{
        cut @.color;
    }
}