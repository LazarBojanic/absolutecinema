var sudoku: int[][] = action int[9][9];
scene printSudoku(var sudoku: int[][]): scrap{
	keepRollingDuring(var i: int = 0; i < 9; i++) {
		keepRollingDuring(var j: int = 0; j < 9; j++) {
			project(sudoku[i][j]);
		}
	}
}
scene entrance(var args: string[]): scrap{
	printSudoku(sudoku);
}